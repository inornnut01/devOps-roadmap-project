# SSH Remote Server Setup

DevOps Projects
Log Archive Tool - https://roadmap.sh/projects/ssh-remote-server-setup

## Setps taken

1. Create EC Instance - AWS
2. Generated two SSH key pairs:

```
  ssh-keygen -t rsa -b 4096 -f ~/.ssh/custom-key-1
  ssh-keygen -t rsa -b 4096 -f ~/.ssh/custom-key-2
```
